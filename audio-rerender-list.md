# מילים לרינדור מחדש · audio-rerender-list

רשימת מילים שנשמעות שגוי בגרסת הסאונד (בדרך כלל: ElevenLabs הגה אותן באנגלית/אמריקאית
במקום רומנית). **לא מתקנים אוטומטית** — הרשימה נאספת, והתיקון נעשה כמכלול.

## איך להוסיף
שורה אחת לכל מילה, בפורמט:

```
- `cuvânt` — מה נשמע במקום מה שצריך (איפה בעמוד)
```

## הרשימה

<!-- להוסיף כאן -->

---

## הערה טכנית לפני שמתקנים (חשוב)

שמות קובצי ה-MP3 הם hash של הטקסט (cyrb53). לכן **רינדור מחדש של אותו טקסט מייצר את אותו
שם קובץ** — מה שאומר:

1. הקובץ החדש ידרוס את הישן ב-repo, אבל
2. אצל כל מי שכבר לחץ "הורד את כל השמע", ה-service worker ימשיך להגיש את **הגרסה הישנה
   מהקאש** — כי ה-URL זהה, וה-headers מסומנים `immutable`.

שלוש דרכים לעקוף, לפי סדר העדפה:

- **לשנות את שם הקאש** ב-`public/sw.js` (`limba-audio-v1` → `v2`). ה-SW מוחק גרסאות ישנות
  ב-activate, והמשתמש יוריד הכול מחדש בלחיצה אחת. פשוט, אבל מוריד את כל 30MB שוב.
- **למחוק נקודתית מהקאש**: להוסיף ל-`sw.js` הודעת `postMessage` שמוחקת מפתחות ספציפיים.
  מדויק, דורש קצת קוד.
- **לשנות מעט את הטקסט** (למשל להוסיף נקודה) — ה-hash משתנה, נוצר קובץ חדש, והישן פשוט
  נשאר יתום. הכי זול, אבל מלכלך את התיקייה.

## כיוונים לתיקון עצמו
- לבדוק אם `language_code: "ro"` בבקשת ה-TTS משפר הגייה של מילים בודדות.
- מילה בודדת מחוץ להקשר היא המקרה הקשה למודל; לפעמים עוזר לרנדר אותה בתוך צירוף קצר
  (`o cafea` במקום `cafea`) — אבל אז משתנה גם הטקסט וגם ה-hash.
- קול חלופי: הקול הנוכחי הוא `onwK4e9ZLuTAKqWW03F9`. יש בפרויקט האפליקציה עוד שלושה
  (Charlotte, Liam, Lizy) — שווה לבדוק אם אחד מהם מדייק יותר ברומנית.
